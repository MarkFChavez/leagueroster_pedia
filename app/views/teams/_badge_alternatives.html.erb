<%#
  This partial demonstrates the three logo badge alternatives available.
  Usage: render 'badge_alternatives', team: @team
%>

<div class="bg-navy-800 border border-lol-gold/20 rounded-lg p-6 space-y-6">
  <h3 class="text-xl font-bold text-white mb-4">Logo Badge Options</h3>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <!-- Hexagon Badge (Primary) -->
    <div class="text-center space-y-3">
      <div class="flex justify-center">
        <%= team_hexagon_badge(team, size: 'large') %>
      </div>
      <p class="text-sm font-bold text-lol-gold">Hexagon Badge</p>
      <p class="text-xs text-gray-400">Sharp angles, esports aesthetic, region-colored gradients with glow effect</p>
    </div>

    <!-- Shield Badge -->
    <div class="text-center space-y-3">
      <div class="flex justify-center">
        <%= team_shield_badge(team, size: 'large') %>
      </div>
      <p class="text-sm font-bold text-lol-gold">Shield Badge</p>
      <p class="text-xs text-gray-400">Traditional sports feel, region label, diagonal pattern overlay</p>
    </div>

    <!-- Geometric Badge -->
    <div class="text-center space-y-3">
      <div class="flex justify-center">
        <%= team_geometric_badge(team, size: 'large') %>
      </div>
      <p class="text-sm font-bold text-lol-gold">Geometric Badge</p>
      <p class="text-xs text-gray-400">Circular with unique pattern per team, clean modern look</p>
    </div>
  </div>

  <div class="pt-4 border-t border-navy-700">
    <p class="text-xs text-gray-500">
      <strong class="text-lol-gold">Region Colors:</strong>
      LCK (Red), LPL (Orange), LEC (Blue), LCS (Purple), PCS (Green), VCS (Dark Orange), CBLOL (Teal), LJL (Pink), LLA (Deep Orange)
    </p>
  </div>
</div>
