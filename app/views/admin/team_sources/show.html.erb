<div>
  <!-- Header Section -->
  <div class="bg-white shadow-sm border border-gray-200 rounded-lg p-6 mb-6">
    <div class="flex justify-between items-center">
      <h2 class="text-3xl font-bold text-gray-900">
        <%= @team_source.short_name %>
      </h2>
      <div class="flex gap-3">
        <%= button_to "Sync Now", sync_admin_team_source_path(@team_source), method: :post, data: { turbo_confirm: "Sync #{@team_source.short_name} from Leaguepedia? This will fetch the latest team and player data." }, class: "bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-2 px-5 rounded-lg transition-colors" %>
        <%= link_to "Edit", edit_admin_team_source_path(@team_source), class: "bg-gray-100 hover:bg-gray-200 text-gray-700 font-semibold py-2 px-5 rounded-lg transition-colors" %>
        <%= button_to "Delete", admin_team_source_path(@team_source), method: :delete, data: { turbo_confirm: "Are you sure you want to delete this team source?" }, class: "bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-5 rounded-lg transition-colors" %>
      </div>
    </div>
  </div>

  <!-- Details Section -->
  <div class="bg-white shadow-sm border border-gray-200 rounded-lg p-6 mb-6">
    <div class="space-y-4">
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Short Name</label>
        <p class="text-gray-900 text-lg"><%= @team_source.short_name %></p>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Long Name</label>
        <p class="text-gray-900 text-lg"><%= @team_source.long_name %></p>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">External Team URL</label>
        <% if @team_source.external_team_url.present? %>
          <%= link_to @team_source.external_team_url, @team_source.external_team_url, target: "_blank", class: "text-yellow-600 hover:text-yellow-700 underline text-lg" %>
        <% else %>
          <p class="text-gray-400 text-lg">Not set</p>
        <% end %>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Last Synced At</label>
        <% if @team_source.last_synced_at.present? %>
          <p class="text-gray-900 text-lg">
            <%= @team_source.last_synced_at.strftime("%B %d, %Y at %I:%M %p") %>
            <span class="text-gray-500 text-sm">(<%= time_ago_in_words(@team_source.last_synced_at) %> ago)</span>
          </p>
        <% else %>
          <p class="text-gray-400 text-lg">Never synced</p>
        <% end %>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Created At</label>
        <p class="text-gray-900"><%= @team_source.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Updated At</label>
        <p class="text-gray-900"><%= @team_source.updated_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      </div>
    </div>
  </div>

  <!-- Back Link -->
  <div>
    <%= link_to "← Back to Team Sources", admin_team_sources_path, class: "text-yellow-600 hover:text-yellow-700 font-medium" %>
  </div>
</div>
