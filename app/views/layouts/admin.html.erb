<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin - League Rosters" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts - Inter %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="relative min-h-screen text-gray-100 antialiased overflow-x-hidden">
    <!-- Animated gradient background with blue admin accent -->
    <div class="fixed inset-0 bg-gradient-to-br from-blue-900 via-[#0D1A28] to-navy-900 animate-[gradientShift_20s_ease_infinite]" style="background-size: 400% 400%; z-index: 0;"></div>

    <!-- Radial glow effect - blue for admin -->
    <div class="fixed top-0 left-1/2 -translate-x-1/2 w-[800px] h-[800px] bg-blue-700/5 rounded-full blur-3xl" style="z-index: 0;"></div>

    <!-- Grid pattern overlay -->
    <div class="fixed inset-0 opacity-[0.03]" style="background-image: linear-gradient(rgba(29, 78, 216, 0.3) 1px, transparent 1px), linear-gradient(90deg, rgba(29, 78, 216, 0.3) 1px, transparent 1px); background-size: 50px 50px; z-index: 0;"></div>

    <main class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 sm:py-8" style="z-index: 1;">
      <%= render 'admin/shared/header' %>
      <%= render 'admin/shared/breadcrumbs' %>
      <%= yield %>
    </main>
  </body>
</html>
